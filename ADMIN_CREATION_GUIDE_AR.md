# دليل إنشاء حساب المدير - الحلول الكاملة

## 📋 الملفات المتوفرة

### 1. للسيرفر Ubuntu (الحل الأفضل)
- `create_admin_server.js` - سكريبت Node.js كامل
- `create_admin_ubuntu.sh` - سكريبت bash للتنفيذ المباشر
- `CREATE_ADMIN_UBUNTU_SERVER.md` - دليل مفصل

### 2. للاختبار المحلي
- `CREATE_ADMIN_WITH_PHONE.js` - للاختبار المحلي
- `CREATE_ADMIN_ON_SERVER.js` - للاختبار عبر API

## 🚀 الطريقة الأسرع (على السيرفر Ubuntu)

### الخطوة 1: رفع الملفات
```bash
# على السيرفر Ubuntu
cd /path/to/your/project
```

### الخطوة 2: تنفيذ السكريبت
```bash
# الطريقة الأولى: Node.js
node create_admin_server.js

# الطريقة الثانية: Bash Script
chmod +x create_admin_ubuntu.sh
./create_admin_ubuntu.sh

# الطريقة الثالثة: curl مباشر
curl -X POST https://www.ab-tw.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"phone":"0501234567","password":"Admin@123456"}'
```

## 🔧 تخصيص البيانات

### في create_admin_server.js:
```javascript
const adminData = {
  phone: '0501234567', // ضع رقم جوالك
  password: 'Admin@123456', // ضع كلمة مرور قوية
  role: 'ADMIN'
};
```

### في create_admin_ubuntu.sh:
```bash
PHONE="0501234567"        # رقم جوالك
PASSWORD="Admin@123456"   # كلمة مرور قوية
```

## ✅ التحقق من النجاح

### 1. اختبار تسجيل الدخول:
```bash
curl -X POST https://www.ab-tw.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"0501234567","password":"Admin@123456"}'
```

### 2. التحقق من الدور:
```bash
# استخدم الـ token من الاستجابة السابقة
curl -X GET https://www.ab-tw.com/api/auth/profile \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

## 🔗 الروابط المهمة

- **الموقع الرئيسي:** https://www.ab-tw.com
- **تسجيل الدخول:** https://www.ab-tw.com/login  
- **لوحة الإدارة:** https://www.ab-tw.com/admin
- **اختبار API:** https://www.ab-tw.com/api/health

## ⚠️ ملاحظات مهمة

1. **غير البيانات الافتراضية** قبل التنفيذ
2. **استخدم كلمة مرور قوية** (أحرف كبيرة + صغيرة + أرقام + رموز)
3. **رقم جوال صحيح** بالصيغة السعودية (05xxxxxxxx)
4. **أول مستخدم** يصبح مدير تلقائياً
5. **غير كلمة المرور** بعد تسجيل الدخول

## 🆘 حل المشاكل

### إذا ظهر "command not found":
```bash
# تثبيت Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# تثبيت dependencies
npm install
```

### إذا ظهر خطأ في قاعدة البيانات:
```bash
# فحص MongoDB
sudo systemctl status mongod

# إعادة تشغيل
sudo systemctl restart mongod
```

### إذا ظهر "رقم الجوال مستخدم":
- استخدم رقم جوال آخر
- أو سجل دخول بالحساب الموجود

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تأكد من أن السيرفر يعمل: `curl https://www.ab-tw.com/api/health`
2. تحقق من ملف `.env` ووجود `MONGODB_URI`
3. تأكد من صحة بيانات قاعدة البيانات
4. راجع logs السيرفر للأخطاء

---

**آخر تحديث:** ديسمبر 2024  
**الحالة:** جاهز للاستخدام ✅