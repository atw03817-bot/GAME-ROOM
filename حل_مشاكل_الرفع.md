# 🔧 حل مشاكل الرفع - mobile-store-vite

## 📊 معلومات المشروع
- **Frontend**: Vite + React
- **Backend**: Express + MongoDB
- **Database**: MongoDB (محلي أو Atlas)

---

## 🎯 المشاكل الشائعة والحلول

### 1️⃣ مشكلة: Frontend لا يتصل بـ Backend

#### الأعراض:
- الموقع يفتح لكن ما يجيب بيانات
- خطأ "Network Error" أو "Failed to fetch"
- المنتجات ما تظهر

#### الحل:
```bash
# 1. تأكد من VITE_API_URL في Vercel
# يجب أن يكون:
VITE_API_URL=https://your-backend-url.com/api

# 2. تأكد Backend يشتغل
# افتح في المتصفح:
https://your-backend-url.com/api/health
# يجب أن يرجع: {"status":"ok",...}
```

---

### 2️⃣ مشكلة: CORS Error

#### الأعراض:
```
Access to fetch at 'https://...' from origin 'https://...' 
has been blocked by CORS policy
```

#### الحل:
✅ **الكود مضبوط بالفعل** (origin: '*')

لكن تأكد من:
```javascript
// في backend/server.js
app.use(cors({
  origin: '*',  // ✅ موجود
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH', 'OPTIONS']
}));
```

---

### 3️⃣ مشكلة: MongoDB لا يتصل

#### الأعراض:
- Backend يطلع خطأ عند التشغيل
- "MongooseError: Connection failed"

#### الحل:

**إذا تستخدم MongoDB محلي:**
```bash
# على السيرفر، تأكد MongoDB شغال:
sudo systemctl status mongod
sudo systemctl start mongod
```

**إذا تستخدم MongoDB Atlas (موصى به):**
```bash
# 1. روح لـ MongoDB Atlas
# 2. Network Access → Add IP Address
# 3. أضف: 0.0.0.0/0 (للسماح لجميع IPs)
# 4. تأكد من Connection String صحيح:
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/mobile-store?retryWrites=true&w=majority
```

---

### 4️⃣ مشكلة: Build يفشل على Vercel

#### الأعراض:
```
Error: Build failed
npm ERR! code ELIFECYCLE
```

#### الحل:
```bash
# 1. جرب Build محلياً أولاً:
cd mobile-store-vite/frontend
npm install
npm run build

# 2. إذا نجح محلياً، المشكلة في إعدادات Vercel
# تأكد من:
# - Root Directory: frontend
# - Build Command: npm run build
# - Output Directory: dist
# - Install Command: npm install
```

---

### 5️⃣ مشكلة: Environment Variables

#### الأعراض:
- API URL غير معرف
- undefined في Console

#### الحل في Vercel:
```bash
# Settings → Environment Variables → Add
VITE_API_URL=https://your-backend-url.com/api

# ⚠️ مهم: بعد إضافة المتغيرات
# اضغط Redeploy!
```

---

### 6️⃣ مشكلة: Backend على AWS لا يشتغل

#### الأعراض:
- Backend URL لا يفتح
- Timeout Error

#### الحل:
```bash
# 1. اتصل بالسيرفر
ssh -i "your-key.pem" ubuntu@your-server-ip

# 2. تأكد من Backend شغال
pm2 status
pm2 logs mobile-store-backend

# 3. إذا مو شغال، شغله:
cd /path/to/mobile-store-vite/backend
pm2 start server.js --name mobile-store-backend

# 4. تأكد من Port مفتوح
sudo ufw status
sudo ufw allow 5000

# 5. تأكد من Nginx مضبوط (إذا تستخدمه)
sudo nginx -t
sudo systemctl status nginx
```

---

### 7️⃣ مشكلة: الصور لا تظهر

#### الأعراض:
- المنتجات تظهر لكن بدون صور
- 404 على الصور

#### الحل:
```bash
# 1. تأكد من مجلد uploads موجود
cd mobile-store-vite/backend
mkdir -p uploads

# 2. تأكد من Static files مضبوط
# في server.js:
app.use('/uploads', express.static('uploads'));

# 3. تأكد من رابط الصورة صحيح
# يجب أن يكون:
https://your-backend-url.com/uploads/image.jpg
```

---

## 🚀 خطوات الرفع الصحيحة

### الطريقة الموصى بها:

#### 1. Frontend على Vercel
```bash
# من Terminal:
cd mobile-store-vite
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/mobile-store-vite.git
git push -u origin main

# ثم على Vercel:
# 1. New Project
# 2. Import من GitHub
# 3. Root Directory: frontend
# 4. Environment Variables:
#    VITE_API_URL=https://your-backend.com/api
# 5. Deploy
```

#### 2. Backend على Railway (الأسهل!)
```bash
# 1. روح لـ https://railway.app
# 2. New Project → Deploy from GitHub
# 3. اختر mobile-store-vite
# 4. Root Directory: backend
# 5. Add Variables:
PORT=5000
NODE_ENV=production
MONGODB_URI=your-mongodb-atlas-uri
JWT_SECRET=your-secret-key
FRONTEND_URL=https://your-vercel-app.vercel.app

# 6. Deploy
# 7. انسخ الـ URL اللي يعطيك إياه
```

#### 3. MongoDB على Atlas (مجاني!)
```bash
# 1. https://www.mongodb.com/cloud/atlas/register
# 2. Create Free Cluster (M0)
# 3. Database Access → Add User
# 4. Network Access → Add IP: 0.0.0.0/0
# 5. Connect → Copy Connection String
# 6. استخدمه في MONGODB_URI
```

#### 4. ربط Frontend بـ Backend
```bash
# في Vercel:
# Settings → Environment Variables
# Edit VITE_API_URL:
VITE_API_URL=https://your-railway-backend.up.railway.app/api

# Redeploy!
```

---

## 🧪 اختبار بعد الرفع

### 1. اختبر Backend
```bash
# في المتصفح:
https://your-backend.com/api/health
# يجب أن يرجع: {"status":"ok"}

https://your-backend.com/api/products
# يجب أن يرجع: قائمة المنتجات
```

### 2. اختبر Frontend
```bash
# افتح موقعك على Vercel
# اضغط F12 → Console
# شوف إذا فيه أخطاء

# اضغط F12 → Network
# شوف الطلبات للـ API
# تأكد ما فيه 404 أو CORS errors
```

### 3. اختبر الوظائف
- [ ] الصفحة الرئيسية تفتح
- [ ] المنتجات تظهر
- [ ] الصور تحمل
- [ ] تسجيل الدخول يشتغل
- [ ] إضافة للسلة يشتغل
- [ ] لوحة الأدمن تفتح

---

## 📝 Checklist النشر

### قبل الرفع:
- [ ] جربت المشروع محلياً (npm run dev)
- [ ] Backend يشتغل محلياً
- [ ] Frontend يشتغل محلياً
- [ ] MongoDB متصل

### أثناء الرفع:
- [ ] الكود على GitHub
- [ ] Frontend على Vercel
- [ ] Backend على Railway/AWS
- [ ] MongoDB على Atlas
- [ ] Environment Variables مضبوطة

### بعد الرفع:
- [ ] Backend URL يفتح
- [ ] Frontend URL يفتح
- [ ] API يرجع بيانات
- [ ] الصور تظهر
- [ ] تسجيل الدخول يشتغل

---

## 🆘 لسه فيه مشكلة؟

**قول لي:**
1. ✅ **إيش الخطأ بالضبط؟** (انسخ رسالة الخطأ)
2. ✅ **وين رفعت المشروع؟** (Vercel / Railway / AWS)
3. ✅ **Backend يشتغل؟** (جرب: your-backend.com/api/health)
4. ✅ **Frontend يفتح؟** (جرب: your-frontend.vercel.app)
5. ✅ **إيش يطلع في Console؟** (F12 → Console)

**وأنا أساعدك خطوة بخطوة! 🚀**
