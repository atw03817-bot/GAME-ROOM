# 🔧 إصلاح مشكلة API في السيرفر الحقيقي

## المشكلة
عند رفع المشروع على السيرفر الحقيقي:
- ❌ الصفحة تظهر loading مستمر
- ❌ خطأ CORS
- ❌ React error #130

## السبب
ملف `.env` في الفرونت اند يشير إلى `localhost`:
```
VITE_API_URL=http://localhost:5000/api
```

هذا يعمل على جهازك المحلي فقط، لكن لا يعمل على السيرفر الحقيقي.

## الحل

### الطريقة 1: تحديث .env قبل الرفع

1. **افتح ملف**: `frontend/.env`

2. **غيّر الرابط** إلى رابط السيرفر الحقيقي:
```env
VITE_API_URL=https://api.ab-he.com/api
```
أو
```env
VITE_API_URL=http://your-server-ip:5000/api
```

3. **أعد بناء المشروع**:
```bash
cd frontend
npm run build
```

4. **ارفع المشروع** من جديد

### الطريقة 2: استخدام Environment Variables

إذا كنت تستخدم Vercel أو خدمة استضافة:

1. اذهب إلى **Settings > Environment Variables**
2. أضف:
   - Key: `VITE_API_URL`
   - Value: `https://api.ab-he.com/api`
3. أعد deploy المشروع

### الطريقة 3: ملفات .env متعددة

أنشئ ملفات مختلفة لكل بيئة:

**frontend/.env.development**
```env
VITE_API_URL=http://localhost:5000/api
```

**frontend/.env.production**
```env
VITE_API_URL=https://api.ab-he.com/api
```

Vite سيختار الملف المناسب تلقائياً.

## التحقق من الإعدادات

### 1. تحقق من رابط API
افتح Console في المتصفح واكتب:
```javascript
console.log(import.meta.env.VITE_API_URL)
```

### 2. تحقق من CORS في الباك اند
تأكد أن ملف `backend/server.js` يحتوي على:
```javascript
app.use(cors({
  origin: ['https://www.ab-he.com', 'https://ab-he.com'],
  credentials: true
}));
```

### 3. تحقق من الباك اند يعمل
افتح في المتصفح:
```
https://api.ab-he.com/api/products
```
يجب أن ترى البيانات.

## خطوات سريعة للإصلاح

```bash
# 1. غيّر .env
cd mobile-store-vite/frontend
# عدّل VITE_API_URL إلى رابط السيرفر الحقيقي

# 2. أعد البناء
npm run build

# 3. ارفع المشروع
# انسخ محتويات dist/ إلى السيرفر
```

## ملاحظات مهمة

⚠️ **لا تنسى**:
- تحديث CORS في الباك اند ليسمح بالدومين الجديد
- التأكد من الباك اند يعمل على السيرفر
- استخدام HTTPS إذا كان متاح
- إعادة بناء المشروع بعد تغيير .env

✅ **بعد الإصلاح**:
- الصفحة ستحمل بشكل طبيعي
- لا أخطاء CORS
- يمكن إضافة منتجات وعمل طلبات
