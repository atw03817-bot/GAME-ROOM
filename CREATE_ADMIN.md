# 👤 إنشاء حساب أدمن

## 🚀 الطريقة السريعة

### 1. شغّل Script إنشاء الأدمن:

```bash
cd backend
node scripts/createAdmin.js
```

---

## 📧 بيانات الدخول

بعد تشغيل الـ script، استخدم هذه البيانات:

```
📧 Email:    admin@store.com
🔑 Password: admin123456
```

---

## 🔗 تسجيل الدخول

1. افتح المتصفح
2. اذهب إلى: `http://localhost:5173/login`
3. أدخل البيانات أعلاه
4. اضغط "تسجيل الدخول"

---

## 🎯 الوصول للوحة التحكم

بعد تسجيل الدخول:

```
http://localhost:5173/admin
```

أو اضغط على "لوحة التحكم" في القائمة

---

## ⚙️ الطريقة اليدوية (إذا لم يعمل الـ script)

### 1. افتح MongoDB Compass أو Mongo Shell

### 2. اذهب لقاعدة البيانات

### 3. افتح collection "users"

### 4. أضف مستخدم جديد:

```json
{
  "name": "Admin",
  "email": "admin@store.com",
  "password": "$2a$10$...", // سيتم تشفيره تلقائياً عند التسجيل
  "phone": "0500000000",
  "role": "admin",
  "createdAt": new Date(),
  "updatedAt": new Date()
}
```

### 5. أو سجل حساب عادي ثم غيّر الـ role:

```javascript
// في Mongo Shell
db.users.updateOne(
  { email: "your-email@example.com" },
  { $set: { role: "admin" } }
)
```

---

## 🔍 التحقق من الحساب

### تحقق من وجود الأدمن:

```bash
# في Mongo Shell
db.users.findOne({ email: "admin@store.com" })
```

يجب أن يظهر:
```json
{
  "_id": "...",
  "name": "Admin",
  "email": "admin@store.com",
  "role": "admin",
  ...
}
```

---

## 🛠️ تغيير كلمة المرور

إذا أردت تغيير كلمة المرور:

### 1. سجل دخول بالحساب الحالي

### 2. اذهب إلى الملف الشخصي

### 3. أو استخدم API:

```bash
curl -X PUT http://localhost:5001/api/auth/profile \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"password":"new-password"}'
```

---

## 📝 ملاحظات

### الأمان:
- ⚠️ غيّر كلمة المرور الافتراضية فوراً
- ⚠️ لا تستخدم هذه البيانات في Production
- ⚠️ استخدم كلمة مرور قوية

### الصلاحيات:
- ✅ الوصول لجميع صفحات الأدمن
- ✅ إدارة المنتجات
- ✅ إدارة الطلبات
- ✅ إدارة العملاء
- ✅ إدارة الإعدادات

---

## 🎉 جاهز!

الآن يمكنك تسجيل الدخول كأدمن والوصول للوحة التحكم!

