🎨 أوامر إصلاح الفافيكون - نسخ والصق:

# 1. الانتقال للمجلد والسحب
cd /var/www/mobile-store
git pull origin main

# 2. تحديث مسارات الفافيكون في قاعدة البيانات
cd backend
node UPDATE_FAVICON_PATHS.js

# 3. إعادة تشغيل السيرفر
cd ..
pm2 restart all

# 4. التحقق من النتيجة
pm2 logs --lines 5

✅ ما تم إصلاحه:

1️⃣ تحديث مسارات الفافيكون:
   - من: https://api.ab-tw.com/uploads/favicon-xxx.png
   - إلى: /uploads/favicon-xxx.png

2️⃣ إضافة hook للتحديث التلقائي:
   - useFaviconUpdater يحدث الأيقونات تلقائياً
   - يضيف timestamp لإجبار المتصفح على إعادة التحميل

3️⃣ تحسين HTML head:
   - إضافة عدة أنواع من الفافيكون
   - دعم أفضل للآيفون والأندرويد

🎯 النتيجة المتوقعة:
- الفافيكون سيظهر في تبويب المتصفح
- أيقونة صحيحة في نتائج جوجل
- أيقونة جميلة عند إضافة الموقع للشاشة الرئيسية (آيفون)

⏰ قد تحتاج لمسح cache المتصفح لرؤية التغيير