# 🚨 تشخيص مشكلة الخادم - CORS & 502 Error

## المشكلة:
- CORS policy errors
- 502 Bad Gateway
- الموقع لا يستطيع الوصول للـ API

## الأسباب المحتملة:
1. الخادم (Backend) لا يعمل
2. مشكلة في إعدادات CORS
3. مشكلة في قاعدة البيانات
4. مشكلة في البورت

## خطوات التشخيص:

### 1. تحقق من حالة الخادم:
```bash
pm2 status
pm2 logs mobile-store-backend
```

### 2. إعادة تشغيل الخادم:
```bash
pm2 restart mobile-store-backend
```

### 3. تحقق من البورت:
```bash
netstat -tulpn | grep :5000
```

### 4. تحقق من قاعدة البيانات:
```bash
mongo
use mobile_store
db.stats()
```

### 5. تحقق من ملف البيئة:
```bash
cat backend/.env
```

### 6. إذا لم يعمل، أعد تشغيل كل شيء:
```bash
pm2 delete all
cd backend
npm start
```

## الحل السريع:
```bash
# في مجلد backend
pm2 restart mobile-store-backend
pm2 logs mobile-store-backend --lines 50
```

## إذا استمرت المشكلة:
1. تحقق من إعدادات CORS في server.js
2. تأكد من أن MongoDB يعمل
3. تأكد من أن البورت 5000 متاح