# دليل إنشاء حساب المدير - الحل النهائي

## 📁 الملفات الجديدة

### 1. السكريبت الرئيسي
- `CREATE_ADMIN_DIRECT_DB.js` - ينشئ المدير مباشرة في قاعدة البيانات

### 2. ملفات التشغيل
- `RUN_CREATE_ADMIN.bat` - للويندوز
- `run_create_admin.sh` - للينكس/ماك

## 🚀 طريقة الاستخدام

### الخطوة 1: تعديل البيانات
افتح ملف `CREATE_ADMIN_DIRECT_DB.js` وغير هذه البيانات:

```javascript
// بيانات المدير - غير هذه البيانات
const ADMIN_DATA = {
  phone: '0501234567',        // ضع رقم جوالك
  password: 'Admin@123456',   // ضع كلمة مرور قوية
  role: 'ADMIN'
};
```

### الخطوة 2: تشغيل السكريبت

#### على الويندوز:
```cmd
RUN_CREATE_ADMIN.bat
```

#### على اللينكس/Ubuntu:
```bash
chmod +x run_create_admin.sh
./run_create_admin.sh
```

#### أو مباشرة:
```bash
node CREATE_ADMIN_DIRECT_DB.js
```

## ✅ المميزات

1. **يعمل مباشرة مع قاعدة البيانات** - لا يحتاج API أو server
2. **يتحقق من المستخدمين الموجودين** - يحدث الدور إذا لزم الأمر
3. **تشفير آمن لكلمة المرور** - باستخدام bcrypt
4. **رسائل واضحة** - يوضح كل خطوة
5. **معالجة الأخطاء** - يعطي حلول للمشاكل الشائعة

## 🔧 متطلبات التشغيل

```bash
npm install mongodb bcryptjs
```

## 📋 مثال على الاستخدام

```bash
# 1. تعديل البيانات في الملف
nano CREATE_ADMIN_DIRECT_DB.js

# 2. تثبيت المتطلبات
npm install mongodb bcryptjs

# 3. تشغيل السكريبت
node CREATE_ADMIN_DIRECT_DB.js
```

## 🔗 بعد إنشاء المدير

1. **سجل دخول**: https://www.gameroom-store.com/login
2. **لوحة الإدارة**: https://www.gameroom-store.com/admin
3. **غير كلمة المرور** من الإعدادات
4. **أضف بيانات المتجر** والإعدادات

## ⚠️ ملاحظات مهمة

- **غير البيانات الافتراضية** قبل التشغيل
- **استخدم كلمة مرور قوية** (أحرف + أرقام + رموز)
- **تأكد من MONGODB_URI** في متغيرات البيئة
- **النسخ الاحتياطي** قبل التعديل على قاعدة البيانات

---

**الحالة:** جاهز للاستخدام ✅  
**آخر تحديث:** ديسمبر 2024