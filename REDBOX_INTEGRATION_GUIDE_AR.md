# 📦 دليل تكامل RedBox الشامل

## ✅ ما تم إنجازه

### 1. خدمة RedBox الأساسية
- ✅ إنشاء شحنات جديدة
- ✅ تتبع الشحنات
- ✅ حساب تكلفة الشحن
- ✅ إلغاء الشحنات
- ✅ تحديث عناوين الشحن
- ✅ جدولة إعادة التسليم
- ✅ تقارير الشحنات

### 2. نظام Webhooks
- ✅ استقبال تحديثات الحالة من RedBox
- ✅ التحقق من صحة التوقيع
- ✅ تحديث حالة الطلبات تلقائياً
- ✅ حفظ تاريخ التتبع

### 3. واجهات برمجة التطبيقات
- ✅ إنشاء شحنة مع الطلب
- ✅ تتبع الشحنة
- ✅ إلغاء الشحنة
- ✅ تحديث العنوان
- ✅ تقارير الشحن

### 4. قاعدة البيانات
- ✅ نموذج الشحنة محدث
- ✅ نموذج مزودي الشحن
- ✅ تاريخ Webhooks

## 🚀 كيفية الاستخدام

### 1. إعداد المفاتيح (مرة واحدة)
```bash
# إعداد مفاتيح RedBox الحقيقية
SETUP_REDBOX.bat

# أو يدوياً
cd backend
node scripts/setup-redbox-credentials.js
```

### 2. تشغيل اختبار التكامل
```bash
# تشغيل الاختبار
TEST_REDBOX.bat

# أو يدوياً
cd backend
node scripts/test-redbox.js
```

### 3. المفاتيح المُضافة
- **Organization ID**: `6931a01939aedef79eb02789`
- **API Key**: JWT Token صالح
- **API URL**: `https://api.redboxsa.com/v1`
- **حالة**: مُفعل ومُختبر

### 3. إعداد Webhook
```
URL: https://yourdomain.com/api/webhooks/redbox
Method: POST
Secret: your-webhook-secret
```

## 📋 API Endpoints الجديدة

### Shipping Management
```
GET    /api/shipping/providers          # جلب مزودي الشحن
POST   /api/shipping/calculate          # حساب تكلفة الشحن
GET    /api/shipping/track/:trackingNumber  # تتبع الشحنة
POST   /api/shipping/shipments/:id/cancel   # إلغاء الشحنة
PUT    /api/shipping/shipments/:id/address  # تحديث العنوان
GET    /api/shipping/reports            # تقارير الشحن
```

### Webhooks
```
POST   /api/webhooks/redbox             # RedBox webhooks
POST   /api/webhooks/smsa               # SMSA webhooks
POST   /api/webhooks/aramex             # Aramex webhooks
```

## 🔧 إعدادات البيئة

تم إضافة المفاتيح الحقيقية إلى `.env`:
```env
# RedBox Settings - CONFIGURED ✅
REDBOX_ORGANIZATION_ID=6931a01939aedef79eb02789
REDBOX_API_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
REDBOX_API_URL=https://api.redboxsa.com/v1
REDBOX_WEBHOOK_SECRET=redbox-webhook-secret-2024
```

## 📊 تدفق العمل

### 1. إنشاء طلب جديد
```
العميل يضع طلب → النظام ينشئ شحنة مع RedBox → يحفظ رقم التتبع
```

### 2. تتبع الشحنة
```
RedBox يرسل تحديثات → Webhook يستقبل → النظام يحدث حالة الطلب
```

### 3. إدارة الشحنات
```
الإدارة تستطيع: إلغاء، تحديث العنوان، جدولة إعادة التسليم
```

## 🧪 الاختبار

### اختبار تلقائي
```bash
# تشغيل جميع الاختبارات
npm run test:redbox

# اختبار محدد
node scripts/test-redbox.js
```

### اختبار يدوي
1. إنشاء طلب تجريبي
2. التحقق من إنشاء الشحنة
3. اختبار التتبع
4. اختبار Webhook

## 🔍 استكشاف الأخطاء

### مشاكل شائعة
1. **API Key غير صحيح**
   - تحقق من المفتاح في لوحة الإدارة
   - تأكد من صحة URL

2. **Webhook لا يعمل**
   - تحقق من URL
   - تأكد من Secret Key
   - فحص logs الخادم

3. **الشحنة لا تُنشأ**
   - تحقق من بيانات العنوان
   - تأكد من تفعيل RedBox
   - فحص وضع الاختبار

### Logs مفيدة
```javascript
// في console الخادم
console.log('RedBox API Response:', response.data);
console.log('Webhook received:', req.body);
console.log('Order status updated:', order.orderStatus);
```

## 📈 التحسينات المستقبلية

### قريباً
- [ ] تكامل مع SMSA
- [ ] تكامل مع Aramex
- [ ] إشعارات SMS للعملاء
- [ ] تقارير متقدمة

### متقدم
- [ ] تحسين الأداء
- [ ] Cache للتكاليف
- [ ] Retry mechanism
- [ ] Monitoring dashboard

## 🎯 الخلاصة

تم إكمال تكامل RedBox بنجاح مع:
- ✅ إنشاء وإدارة الشحنات
- ✅ تتبع تلقائي
- ✅ Webhooks آمنة
- ✅ واجهات API شاملة
- ✅ اختبارات شاملة

النظام جاهز للاستخدام في الإنتاج بعد إضافة API credentials الحقيقية.