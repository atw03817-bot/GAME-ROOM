# 🔄 إعادة تشغيل كاملة للخادم

## الأوامر للنسخ واللصق:

### 1. إيقاف جميع العمليات:
```bash
pm2 delete all
```

### 2. الذهاب لمجلد المشروع:
```bash
cd /home/ubuntu/mobile-store-vite
```

### 3. سحب آخر التحديثات:
```bash
git pull origin main
```

### 4. الذهاب لمجلد backend:
```bash
cd backend
```

### 5. تشغيل الخادم من جديد:
```bash
pm2 start server.js --name mobile-store-backend
```

### 6. التحقق من الحالة:
```bash
pm2 status
```

### 7. مشاهدة السجلات:
```bash
pm2 logs mobile-store-backend --lines 20
```

## إذا أردت تشغيل مباشر للاختبار:
```bash
cd /home/ubuntu/mobile-store-vite/backend
node server.js
```

## النتيجة المتوقعة:
- ✅ لا توجد أخطاء ERR_MODULE_NOT_FOUND
- ✅ الخادم يعمل على البورت 5000
- ✅ رسالة "Server running on http://localhost:5000"
- ✅ رسالة "MongoDB متصل بنجاح"

**انسخ والصق الأوامر واحد تلو الآخر!**