# 📦 دليل بناء ملف AAB لـ Google Play

## الطريقة 1: استخدام الملف الجاهز (الأسهل)

1. **افتح ملف:** `BUILD_AAB.bat`
2. **انتظر** حتى تكتمل العملية (10-20 دقيقة)
3. **سجل الدخول** إلى حساب Expo عند الطلب
4. **حمّل الملف** من الرابط الذي سيظهر

---

## الطريقة 2: يدوياً

### 1. تثبيت EAS CLI (مرة واحدة فقط)
```bash
npm install -g eas-cli
```

### 2. تسجيل الدخول
```bash
eas login
```

### 3. بناء AAB
```bash
cd OliviaShip-Expo-App
eas build --platform android --profile production
```

### 4. تحميل الملف
- بعد اكتمال البناء، ستحصل على رابط
- حمّل ملف `app-release.aab`

---

## ملاحظات مهمة:

### ✅ تم التعديل:
- `eas.json` تم تعديله لبناء AAB بدلاً من APK
- الإعدادات جاهزة للنشر على Google Play

### ⚠️ متطلبات:
- حساب Expo (مجاني)
- اتصال إنترنت جيد
- صبر (البناء يتم على سيرفرات Expo)

### 📱 معلومات التطبيق:
- **Package Name:** com.oliviaship.app
- **Version:** 1.0.0
- **Version Code:** 1

---

## إذا واجهت مشاكل:

### مشكلة: "eas: command not found"
**الحل:**
```bash
npm install -g eas-cli
```

### مشكلة: "Not logged in"
**الحل:**
```bash
eas login
```

### مشكلة: "Build failed"
**الحل:**
- تحقق من اتصال الإنترنت
- تحقق من صحة `app.json`
- راجع سجل الأخطاء

---

## بعد تحميل AAB:

1. اذهب إلى Google Play Console
2. اذهب إلى **Production** > **Create new release**
3. ارفع ملف `app-release.aab`
4. أكمل باقي الإعدادات
5. اضغط **Review and rollout**

---

**بالتوفيق! 🚀**
