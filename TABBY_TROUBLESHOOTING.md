# 🔧 حل مشاكل Tabby - دليل التشخيص

## المشكلة: Tabby لا تظهر في صفحة الدفع

### ✅ الخطوات المطلوبة للحل:

## 1. تحقق من إعدادات قاعدة البيانات
```bash
# شغل هذا السكريبت للتحقق من قاعدة البيانات
node CHECK_TABBY_SETTINGS.js
```

## 2. تحقق من API مباشرة
- افتح `TEST_PAYMENT_METHODS_API.html` في المتصفح
- اضغط على "اختبار جلب طرق الدفع"
- تأكد من أن Tabby موجود في النتائج

## 3. تحقق من console الخادم
عند تشغيل الخادم، يجب أن ترى هذه الرسائل:
```
🔍 Checking payment setting: tabby, enabled: true
✅ Adding Tabby to payment methods
📋 Available payment methods: [...]
```

## 4. تحقق من console المتصفح
في صفحة الدفع، افتح Developer Tools وتحقق من:
```
🔍 Fetching payment methods...
📋 Payment methods received: [...]
🔍 Tabby found in methods: true/false
```

## 5. الأسباب المحتملة والحلول:

### السبب 1: إعدادات Tabby غير محفوظة بشكل صحيح
**الحل:**
1. اذهب إلى `/admin/tabby-payment-settings`
2. تأكد من إدخال جميع البيانات:
   - Public Key: `pk_test_01968174-594d-7042-be8f-f9d25036ec54`
   - Secret Key: `sk_test_01968174-594d-7042-be8f-f9d2b3c79dce`
   - Merchant Code: `top1`
3. اضغط "اختبار الاتصال" - يجب أن يكون ناجحاً
4. تأكد من تفعيل Tabby (الزر الأخضر)
5. احفظ الإعدادات

### السبب 2: الخادم لم يتم إعادة تشغيله
**الحل:**
```bash
# أوقف الخادم (Ctrl+C)
# ثم شغله مرة أخرى
npm run dev
```

### السبب 3: مشكلة في cache المتصفح
**الحل:**
- اضغط `Ctrl+Shift+R` لإعادة تحميل الصفحة بدون cache
- أو امسح cache المتصفح

### السبب 4: خطأ في API
**الحل:**
1. تحقق من console الخادم للأخطاء
2. تأكد من أن الخادم يعمل على المنفذ الصحيح
3. اختبر API مباشرة باستخدام `TEST_PAYMENT_METHODS_API.html`

## 6. اختبار سريع:

### اختبار 1: API مباشر
```bash
curl http://localhost:5000/api/payments/methods
```
يجب أن ترى Tabby في النتائج.

### اختبار 2: قاعدة البيانات
```bash
node CHECK_TABBY_SETTINGS.js
```
يجب أن ترى "✅ Tabby settings found" و "Enabled: true"

### اختبار 3: Frontend
1. افتح صفحة الدفع
2. افتح Developer Tools (F12)
3. اذهب إلى Console
4. ابحث عن رسائل "Payment methods received"
5. تأكد من وجود Tabby في القائمة

## 7. إذا لم تنجح الحلول السابقة:

### إعادة إنشاء إعدادات Tabby:
```javascript
// شغل هذا في MongoDB Compass أو mongo shell
db.paymentsettings.deleteOne({provider: "tabby"});
db.paymentsettings.insertOne({
  provider: "tabby",
  enabled: true,
  config: {
    publicKey: "pk_test_01968174-594d-7042-be8f-f9d25036ec54",
    secretKey: "sk_test_01968174-594d-7042-be8f-f9d2b3c79dce",
    merchantCode: "top1",
    apiUrl: "https://api.tabby.ai"
  }
});
```

## 8. التحقق النهائي:
بعد تطبيق الحلول، يجب أن ترى:
- ✅ Tabby في صفحة الإعدادات (مفعل)
- ✅ Tabby في نتائج API `/payments/methods`
- ✅ Tabby في صفحة الدفع كخيار متاح
- ✅ رسائل console تؤكد وجود Tabby

## 🆘 إذا استمرت المشكلة:
1. تأكد من أن جميع الملفات محفوظة
2. أعد تشغيل الخادم والمتصفح
3. تحقق من أن لا توجد أخطاء في console
4. جرب في متصفح آخر أو نافذة خاصة