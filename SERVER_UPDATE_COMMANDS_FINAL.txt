# 🚀 تحديث الخادم - الأوامر النهائية

## المشكلة:
الخادم لم يسحب التحديثات الجديدة التي تحتوي على الإصلاحات

## الحل - نفذ هذه الأوامر بالترتيب:

### 1. توقف الخادم:
```bash
pm2 stop mobile-store-backend
```

### 2. اذهب لمجلد المشروع:
```bash
cd /home/ubuntu/mobile-store-vite
```

### 3. اسحب التحديثات الجديدة:
```bash
git pull origin main
```

### 4. اذهب لمجلد backend:
```bash
cd backend
```

### 5. أعد تشغيل الخادم:
```bash
pm2 start server.js --name mobile-store-backend
```

### 6. تحقق من السجلات:
```bash
pm2 logs mobile-store-backend --lines 10
```

## إذا استمر الخطأ:

### خيار 1 - إعادة تشغيل كاملة:
```bash
pm2 delete mobile-store-backend
cd /home/ubuntu/mobile-store-vite/backend
pm2 start server.js --name mobile-store-backend
```

### خيار 2 - تشغيل مباشر للاختبار:
```bash
cd /home/ubuntu/mobile-store-vite/backend
node server.js
```

## النتيجة المتوقعة:
- ✅ الخادم يعمل بدون أخطاء
- ✅ لا توجد رسائل ERR_MODULE_NOT_FOUND
- ✅ الموقع يعمل بشكل طبيعي

**المهم: تأكد من سحب التحديثات الجديدة أولاً!**